package com.example.demo21.repository;

import com.example.demo21.domain.Reward;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepositoryReward extends JpaRepository<Reward,Long> {
}
